import React from 'react';
import ReactDOM from 'react-dom';

const firstReactElement = React.createElement('h1', {}, 'Hello WURRRRLD');

ReactDOM.render(firstReactElement, document.getElementById('root'));
