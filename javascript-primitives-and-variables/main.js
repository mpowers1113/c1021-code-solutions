var fullName = 'Matt Powers';

var isCool = true;

var totalPets = 0;

var unknown = null;

var never;

console.log('never', never, typeof never);

console.log('full name', fullName, typeof fullName);

console.log('is cool', isCool, typeof isCool);

console.log('total pets', totalPets, typeof totalPets);

console.log('unknown', unknown, typeof unknown);
